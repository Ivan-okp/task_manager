Task Manager API

REST API на FastAPI для управления пользователями и задачами. Проект разделён на две части:

· Демонстрационная — полный CRUD для пользователей и задач (для администрирования)
· Сервисная — реальные сценарии с аутентификацией через JWT

📋 Содержание

· Требования
· 🚀 Быстрый старт
· 1. Клонирование репозитория
· 2. Настройка окружения
· 3. Установка зависимостей
· 4. Настройка переменных окружения
· 5. Запуск приложения
· 🐳 Запуск через Docker
· 1. Сборка и запуск контейнеров
· 2. Остановка контейнеров
· 📚 API Endpoints
· Демонстрационные эндпоинты
· Сервисные эндпоинты
· 🔐 Аутентификация
· 1. Регистрация пользователя
· 2. Получение JWT токена
· 3. Использование токена
· 🧪 Тестирование
· Запуск тестов
· Структура тестов
· 🔧 Инструменты разработки
· Linting и Formatting
· Pre-commit хуки
· Настройка pre-commit
· ⚙️ Настройки окружения
· Файл .env.example
· Основные переменные
· 📁 Структура проекта
· 🤝 Контрибьюция

🛠 Требования

· Python 3.9+
· Poetry (для управления зависимостями)
· Docker и Docker Compose (для контейнеризации)
· Git

🚀 Быстрый старт

1. Клонирование репозитория
   git clone <repository-url>
   cd task_manager

2. Настройка окружения

Создайте и активируйте виртуальное окружение:

Linux/macOS:
python -m venv venv
source venv/bin/activate

Windows:
python -m venv venv
venv\Scripts\activate

3. Установка зависимостей

# Установка Poetry (если не установлен)

curl -sSL https://install.python-poetry.org | python3 -

# Установка зависимостей проекта

poetry install

# Или установка без dev-зависимостей

poetry install --no-dev

4. Настройка переменных окружения

# Скопируйте пример файла окружения (.env.example уже содержит все необходимые заготовки)

cp .env.example .env

# Отредактируйте .env файл под ваши нужды

# Все основные переменные уже предварительно настроены в .env.example

5. Запуск приложения

# Запуск сервера разработки с авто-перезагрузкой

uvicorn src.task_manager.main:app --reload --host 0.0.0.0 --port 8000

После запуска откройте в браузере:

· 📚 Swagger UI: http://localhost:8000/docs
· 📖 ReDoc: http://localhost:8000/redoc

🐳 Запуск через Docker

1. Сборка и запуск контейнеров

# Собрать и запустить все сервисы

docker-compose up --build

# Запуск в фоновом режиме

docker-compose up -d --build

# Просмотр логов

docker-compose logs -f

# Проверить статус контейнеров

docker-compose ps

2. Остановка контейнеров

# Остановка с удалением контейнеров

docker-compose down

# Остановка с удалением контейнеров и volumes

docker-compose down -v

# Остановка с удалением контейнеров, volumes и образов

docker-compose down -v --rmi all

📚 API Endpoints

Демонстрационные эндпоинты

Пользователи:
GET /users - Получить список всех пользователей
GET /users/{user_id} - Получить пользователя по ID
POST /users - Создать нового пользователя
PUT /users/{user_id} - Обновить пользователя
DELETE /users/{user_id} - Удалить пользователя

Задачи:
GET /tasks - Получить список всех задач
GET /tasks/{task_id} - Получить задачу по ID
POST /tasks - Создать новую задачу
PUT /tasks/{task_id} - Обновить задачу
DELETE /tasks/{task_id} - Удалить задачу

Сервисные эндпоинты

Пользователи:
POST /service_user/create_user - Регистрация нового пользователя
POST /service_user/login - Вход и получение JWT токена
PUT /service_user/change_user - Изменение данных текущего пользователя
DELETE /service_user/delete_user - Удаление текущего пользователя

Задачи:
GET /service/get_all_tasks - Получить все задачи текущего пользователя
GET /service/get_specific_task - Получить конкретную задачу (по ID или title)
POST /service/create_task - Создать новую задачу
PUT /service/update_task - Обновить задачу
DELETE /service/delete_task - Удалить задачу

🔐 Аутентификация

1. Регистрация пользователя
   curl -X POST "http://localhost:8000/service_user/create_user" \
   -H "Content-Type: application/json" \
   -d '{"name": "John Doe",
   "email": "john@example.com",
   "password": "securepassword123"
   }'

2. Получение JWT токена
   curl -X POST "http://localhost:8000/service_user/login" \
   -H "Content-Type: application/x-www-form-urlencoded" \
   -d "username=john@example.com&password=securepassword123"

Ответ:
{
"access_token": "eyJhbGciOiJIUzI1NiIs...",
"token_type": "Bearer"
}

3. Использование токена

Добавьте заголовок Authorization к защищенным запросам:

# Пример получения задач текущего пользователя

curl -X GET "http://localhost:8000/service/get_all_tasks" \
-H "Authorization: Bearer eyJhbGciOiJIUzI1NiIs..."

🧪 Тестирование

Запуск тестов

# Запуск всех тестов

pytest

# Запуск тестов с детальным выводом

pytest -v

# Запуск тестов с покрытием

pytest --cov=src.task_manager tests/

# Запуск конкретного тестового файла

pytest tests/test_cases/test_users.py

# Запуск тестов с генерацией отчета

pytest --html=report.html --self-contained-html

# Запуск тестов в Docker

docker-compose exec web pytest

Структура тестов
tests/
├── test_cases/ # Основные тестовые случаи
├── tests_for_demo/ # Тесты для демо-эндпоинтов
├── tests_for_service/ # Тесты для сервисных эндпоинтов
├── conftest.py # Фикстуры pytest
└── test_database.py # Настройка тестовой БД

🔧 Инструменты разработки

Linting и Formatting

# Запуск ruff для проверки кода

ruff check .

# Автоматическое исправление ошибок

ruff check . --fix

# Форматирование кода

ruff format .

# Запуск всех проверок качества кода

ruff check . && ruff format --check . && mypy src/

Pre-commit хуки

Конфигурация в .pre-commit-config.yaml включает:

1. Trailing whitespace fixer - удаление лишних пробелов
2. End of file fixer - добавление пустой строки в конце файла
3. Ruff - линтинг и форматирование
4. Mypy - проверка типов

Настройка pre-commit

# Установка pre-commit

pip install pre-commit

# Установка git хуков

pre-commit install

# Запуск проверки на всех файлах

pre-commit run --all-files

# Запуск проверки при коммите (автоматически)

git commit -m "Your message"

# Пропуск проверок (если необходимо)

git commit -m "Your message" --no-verify

# Обновление хуков до последних версий

pre-commit autoupdate

⚙️ Настройки окружения

Файл .env.example

Проект уже содержит файл .env.example с готовыми заготовками для всех необходимых переменных окружения. Этот файл
включает:

· Предварительно настроенные значения для локальной разработки
· Комментарии к каждой переменной
· Примеры значений для всех настроек

Основные переменные

После копирования .env.example в .env, вы можете настроить следующие переменные:

# ===== БАЗА ДАННЫХ =====

POSTGRES_USER
POSTGRES_PASSWORD
POSTGRES_HOST
POSTGRES_NAME

# ===== JWT АУТЕНТИФИКАЦИЯ =====

SECRET_KEY
ALGORITHM
ACCESS_TOKEN_EXPIRE_MINUTES

📁 Структура проекта
task_manager/
├── src/
│ ├── task_manager/
│ │ ├── database.py # Настройка базы данных
│ │ ├── logger.py # Логирование
│ │ ├── models/ # SQLAlchemy модели
│ │ ├── repositories/ # Репозитории для работы с БД
│ │ ├── routers/ # Роутеры FastAPI
│ │ ├── schemas/ # Pydantic схемы
│ │ ├── security.py # Аутентификация и JWT
│ │ ├── __init__.py
│ │ └── main.py # Точка входа приложения
├── tests/ # Тесты
├── .env.example # Шаблон переменных окружения (готовые заготовки)
├── .gitignore
├── .pre-commit-config.yaml
├── docker-compose.yaml # Конфигурация для Docker Compose
├── Dockerfile # Docker образ приложения
├── poetry.lock # Lock файл зависимостей
├── pyproject.toml # Конфигурация проекта и зависимости
├── pytest.ini # Конфигурация pytest
└── README.md # Документация

🤝 Контрибьюция

1. Форкните репозиторий
2. Создайте ветку для новой функции (git checkout -b feature/amazing-feature)
3. Зафиксируйте изменения (git commit -m 'Add amazing feature')
4. Запушьте в ветку (git push origin feature/amazing-feature)
5. Откройте Pull Request

Перед коммитом убедитесь, что:

· Все тесты проходят
· Код соответствует стилю (ruff check проходит)
· Добавлены тесты для новой функциональности
· Обновлена документация при необходимости
· Pre-commit хуки успешно выполняются
